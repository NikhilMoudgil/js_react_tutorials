import React from "react";
const UserContext = React.createContext()

export default UserContext;
//every context is  a provider
// The components in the usercontext can directly access all states through userContext